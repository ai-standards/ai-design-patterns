import { PatternTitlePipe } from './pattern-title-pipe';

describe('PatternTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new PatternTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
