import { describe, it, expect } from 'vitest';
import { example } from './index';

describe('PathScore', () => {
  it('should work', () => {
    expect(example()).toBe('Hello from PathScore!');
  });
});
