import { describe, it, expect } from 'vitest';
import { example } from './index';

describe('Agentic Redux', () => {
  it('should work', () => {
    expect(example()).toBe('Hello from Agentic Redux!');
  });
});
